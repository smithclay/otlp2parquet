# Cargo configuration for otlp2parquet
#
# This file configures cross-compilation targets for building Lambda binaries
# on x86_64 Linux (GitHub Actions) for different architectures.

# aarch64 (ARM64) cross-compilation for AWS Lambda
[target.aarch64-unknown-linux-gnu]
linker = "aarch64-linux-gnu-gcc"
