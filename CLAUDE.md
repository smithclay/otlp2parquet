see @AGENTS.md
- use "make build-cli" to build the CLI binary for desktop use
- use "make build-lambda" to build lambda functions
- prefer using the makefile over cargo commands
- always use the uv tool to run python or uvx to run python executable scripts/CLIs
