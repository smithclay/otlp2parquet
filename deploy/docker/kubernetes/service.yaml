apiVersion: v1
kind: Service
metadata:
  name: otlp2parquet
  labels:
    app: otlp2parquet
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: otlp2parquet
---
# Optional: Ingress for external access
# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: otlp2parquet
#   annotations:
#     kubernetes.io/ingress.class: nginx
#     cert-manager.io/cluster-issuer: letsencrypt-prod
# spec:
#   tls:
#   - hosts:
#     - logs.example.com
#     secretName: otlp2parquet-tls
#   rules:
#   - host: logs.example.com
#     http:
#       paths:
#       - path: /
#         pathType: Prefix
#         backend:
#           service:
#             name: otlp2parquet
#             port:
#               number: 8080
