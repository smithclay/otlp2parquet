# Example secret configuration
# DO NOT commit this file with real credentials!
# Copy to secret.yaml and fill in your credentials, then apply with kubectl

apiVersion: v1
kind: Secret
metadata:
  name: otlp2parquet-secrets
  labels:
    app: otlp2parquet
type: Opaque
stringData:
  # AWS S3 credentials (base64 encode in production)
  aws_access_key_id: "AKIAIOSFODNN7EXAMPLE"
  aws_secret_access_key: "wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY"

  # Cloudflare R2 credentials
  # r2_access_key_id: "your_r2_access_key_id"
  # r2_secret_access_key: "your_r2_secret_access_key"

---
# Best practice: Use external secret management
# Example with External Secrets Operator:
#
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: otlp2parquet-secrets
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: aws-secrets-manager
#     kind: SecretStore
#   target:
#     name: otlp2parquet-secrets
#     creationPolicy: Owner
#   data:
#   - secretKey: aws_access_key_id
#     remoteRef:
#       key: otlp2parquet/aws
#       property: access_key_id
#   - secretKey: aws_secret_access_key
#     remoteRef:
#       key: otlp2parquet/aws
#       property: secret_access_key
