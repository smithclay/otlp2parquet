# Cloudflare Workers smoke test credentials
# Get these from: https://dash.cloudflare.com

# Required for wrangler deployment
CLOUDFLARE_API_TOKEN=your_api_token_here
CLOUDFLARE_ACCOUNT_ID=your_account_id_here

# R2 bucket for smoke tests (must be pre-created manually)
# Create at: https://dash.cloudflare.com -> R2 -> Create bucket
CLOUDFLARE_BUCKET_NAME=otlp2parquet-smoke

# R2 S3-compatible API credentials (required for writing to R2, uses AWS standard names)
# Create at: https://dash.cloudflare.com -> R2 -> Manage R2 API Tokens
# Choose "Admin Read & Write" permissions
AWS_ACCESS_KEY_ID=your_r2_access_key_id_here
AWS_SECRET_ACCESS_KEY=your_r2_secret_access_key_here

# R2 S3-compatible endpoint (optional, will be auto-constructed from CLOUDFLARE_ACCOUNT_ID if not set)
# AWS_ENDPOINT_URL=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
